# Copy this file to .env (which is gitignored) OR set these as secrets in your
# Claude Code web environment's environment-variable settings.
# NEVER paste real tokens into chat or commit them.

# Apify API token — used by the Apify MCP server (.mcp.json) for LinkedIn viral sourcing.
APIFY_TOKEN=your-apify-token-here
